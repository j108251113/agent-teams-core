# Agent Teams 實作清單

專案目標：參考 Overstory 與 Copilot SDK，打造一個在「GPT-5 Mini」(低上下文容量) 環境下也能穩定運作的 Agent Teams 框架。

## 第三方參考
- **Overstory**: 使用 `git worktree` 進行任務隔離，SQLite 郵件系統進行通訊，動態指令注入 (Overlay)。
- **Copilot SDK**: 透過 Node.js SDK 進行 Copilot CLI 的程式化控制。

## 階段一：基礎建設 (Infrastructure)

### 1. 專案初始化
- [x] 初始化 TypeScript 專案 (ESM, 嚴格模式)
- [x] 設定測試框架 (Vitest)
- [x] 設定 Linting & Formatting (Biome)
- [x] 建立 [copilot-instructions.md](file:///f:/repos/agent-teams-core/.github/copilot-instructions.md)
- [x] 重置遠端倉庫並強制推送 (Force Push)
- **驗收條件**: `npm run test`, `npm run lint`, `npm run typecheck` 皆透過。
- **測試點**: 空的測試案例可執行。

### 2. 工作區管理員 (WorktreeManager)
- [x] 實作 Git Worktree 的創建、刪除與重置邏輯。
- [x] 實作工作區清理機制，避免磁碟空間浪費。
- **驗收條件**: 成功在子目錄創建獨立於 main 分支的工作區，並能正確清理。
- **整合測試**: 在 Temp 目錄測試 `git worktree add` 與 `remove`。
- **驗收測試**: 模擬一個任務分支的生命週期。

## 階段二：低上下文隔離通訊 (Low-Context Messaging)

### 3. SQLite 通訊系統 (MessageStore)
- [x] 使用 `better-sqlite3` 或 `bun:sqlite` 實作訊息佇列。
- [x] 支援 `Status`, `Question`, `Result`, `Error` 類型的訊息。
- **驗收條件**: Agent A 發送訊息後，Agent B (或 Orchestrator) 能讀取到，支援 WAL 模式。
- **整合測試**: 多服務同時寫入/讀取 SQLite 的並行測試。
- **驗收測試**: 訊息傳遞達成一次完整的提問與回答。

### 4. 上下文監控與重啟機制 (ContextManager)
- [x] 實作 Token 計算或上下文長度預估邏輯。
- [x] 實作「上下文清零」機制：當偵測到上下文即將溢出，存檔目前狀態，清空歷史，並提示 Agent 從 Checkpoint 恢復。
- [x] 實作「代理人重啟」：當上下文太亂時，直接啟動一個全新的代理人實例。
- **驗收條件**: 超過設定限額時，系統能觸發清零或重啟邏輯。
- **整合測試**: 模擬大量對話觸發清零逻辑。
- **驗收測試**: 代理人在清零後能依據 Checkpoint 繼續執行任務。

## 階段三：編排器與任務流 (TDD 驅動)

### 5. Copilot SDK 整合與指令注入 (Instruction Overlay)
- [ ] **TDD**: 定義 `CopilotAgent` 與 `Overlay` 的整合測試案例。
- [ ] 整合 `@github/copilot-sdk`：封裝 CopilotSession 的生命週期。
- [ ] 實作指令生成器 (Overlay)：根據任務、角色在 Worktree 建立專屬 `.github/copilot-instructions.md`。
- [ ] 實作信標機制 (Beacon Protocol)：自動化發送首條引導訊息，觸發 Agent 讀取指令。
- **驗收條件**: 啟動 Agent 後，它能正確讀取到該任務的工作區指令並開始執行任務。

### 6. 多代理人編排與整合 (Orchestration)
- [ ] **TDD**: 定義 Orchestrator 完整分派流程的驗收測試。
- [ ] 任務分派：Orchestrator 自動建立 Worktree -> 寫入 Overlay -> 啟動 CopilotAgent。
- [ ] 整合 `MessageStore` 與 `Agent` 模型。
- **驗收條件**: Orchestrator 能指派任務給 Worker 且正確處理回傳結果。
- **整合測試**: 測試 Orchestrator 的任務分派與狀態更新。

## 階段四：整合驗收 (Final E2E Foundation)

### 7. 核心基礎架構驗收 (Infrastructure Acceptance)
- [ ] **TDD**: 實作 E2E 整合測試：模擬一個真實的開發任務流。
- [ ] 完成 Agent Swarm 的核心基礎架構（Worktree, SQLite, Context, Copilot SDK, Orchestration）。
- **驗收條件**: 所有核心組件皆有整合測試覆蓋且全數通過。
